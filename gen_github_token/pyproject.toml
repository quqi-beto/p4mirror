[project]
name = "gen-github-token"
version = "0.1.0"
description = "Generate a GitHub App installation access token (ghs_...) for testing p4mirror locally."
requires-python = ">=3.10"
dependencies = [
    "pyjwt[crypto]>=2.0",
    "requests>=2.31",
]
